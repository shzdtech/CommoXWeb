import requirementList from '../requirementList';

const requirements = (state = requirementList, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default requirements;